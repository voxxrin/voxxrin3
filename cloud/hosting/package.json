{
  "name": "voxxrin-firebase-hosting",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "copy-from-mobile": "rm -Rf ./public/* && cp -a ../../mobile/dist/* ./public/",
    "deploy": "npm run copy-from-mobile && firebase use voxxrin-v3 && firebase deploy --only hosting:voxxrin-v3",
    "deploy:demo": "npm run copy-from-mobile && firebase use voxxrin-v3-demo && firebase deploy --only hosting:voxxrin-v3-demo"
  },
  "license": "Apache-2.0",
  "devDependencies": {
    "firebase-tools": "12.3.1"
  }
}
