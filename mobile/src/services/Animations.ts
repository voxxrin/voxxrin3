import {Temporal} from "temporal-polyfill";

export const TimeslotAnimations = {
    ANIMATION_BASE_DELAY: Temporal.Duration.from({ milliseconds: 50 }),
    ANIMATION_DURATION: Temporal.Duration.from({ milliseconds: 150 }),
}
